import Head from 'next/head';
import styles from '@/styles/Auth.module.css';
import Auth from '@/components/Auth';
import { useState } from 'react';
import Image from 'next/image';
import Yeti from '../public/test1.png';
import VideoChat from '../public/test4.png';

export default function Login() {
  const [authType, setAuthType] = useState<
    'login' | 'register' | 'resetPassword'
  >('login');

  return (
    <>
      <Head>
        <title>FrostByte</title>
        <meta name="description" content="Generated by create next app" />
        <meta name="viewport" content="width=device-width, initial-scale=1" />
        <link rel="icon" href="/favicon.ico" />
      </Head>

      <main className={`${styles.mainBackground} xl:bg-grey-600`}>
        <div
          className={`${styles.authMD} items-center overflow-auto h-screen w-full `}
        >
          <div
            className={`${styles.mainContainer} col-start-4 col-end-10 row-start-2 row-end-3 flex w-full h-full  xl:rounded-3xl`}
          >
            <div className=" w-full hidden xl:flex xl:flex-col h-full ">
              <div
                className={`${styles.frostCordTitle} flex justify-center items-center text-6xl font-extrabold  `}
              >
                <div className={`${styles.frostCord} tracking-wider`}>
                  FrostByte
                </div>
              </div>
              <div className="flex justify-between ml-9 mt-6 mb-6 basis-3/4 z-10 relative">
                <Image
                  className={`${styles.images} w-auto rounded-xl h-12`}
                  src={Yeti}
                  alt="Yeti"
                  priority
                />
                <Image
                  className={`${styles.images} rounded-xl w-auto h-13  absolute left-10 top-10`}
                  src={VideoChat}
                  alt="Video"
                  priority
                />
                <div
                  className={`${styles.textLeft} text-grey-200  absolute top-[324px] left-4 w-9 tracking-wider font-medium text-sm`}
                >
                  Join the conversation
                </div>
                <div
                  className={`${styles.textRight} text-grey-200 absolute top-5 left-12 tracking-wider font-medium text-sm`}
                >
                  Unite, chat, and conquer together.
                </div>
              </div>
            </div>
            <div
              className={`flex flex-col w-full  items-center xl:rounded-3xl ${styles.rightSide} z-50`}
            >
              <div className="basis-1/4 flex items-center relative mt-7">
                <Image
                  className={`rounded-full  ${styles.logo}`}
                  src="/favicon.ico"
                  alt="FrostCord Logo"
                  width={120}
                  height={37}
                  priority
                />
              </div>
              <Auth type={authType} setAuthType={setAuthType} />
            </div>
          </div>
        </div>
      </main>
    </>
  );
}
